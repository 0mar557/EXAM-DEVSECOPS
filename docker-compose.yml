services:
  api:
    build: .
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - taskflow-net

  redis:
    image: redis:7-alpine
    container_name: taskflow-redis
    networks:
      - taskflow-net

networks:
  taskflow-net:
    driver: bridge
